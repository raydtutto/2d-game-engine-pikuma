set(MODULE_SOURCE)

file(GLOB_RECURSE MODULE_SOURCE
        CONFIGURE_DEPENDS
        ${CMAKE_CURRENT_SOURCE_DIR}/*.[hc]
        ${CMAKE_CURRENT_SOURCE_DIR}/*.[hc]pp)

set(LIBS_SOURCES ${LIBS_SOURCES} ${MODULE_SOURCE} PARENT_SCOPE)